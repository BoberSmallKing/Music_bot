version: "3.9"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./downloads:/app/downloads
      - ./bot_session.session:/app/bot_session.session
    # Если на сервере мало памяти, можно ограничить:
    # deploy:
    #   resources:
    #     limits:
    #       memory: 512M